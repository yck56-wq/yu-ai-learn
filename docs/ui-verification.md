# MVP 草图页面还原验收

本次参考：`.superpowers/brainstorm/1636-1788354742/content/mvp-core-sketch-v2.html`。

- 首页、备题、作答、答对反馈、答错反馈、通关复盘均迁移至 Taro 组件。
- 原稿颜色、字体栈、气泡尾部、30px 黄色图标、选项字母圆圈、描边、偏移阴影、便签和掌握度圆环使用共用样式。
- designWidth 改为 375，解决原先字号缩小一半的问题。真实设备顶部使用状态栏/胶囊尺寸，底部使用安全区；不绘制原型外部手机边框或假时间。
- 保留两个案例按钮；不添加未完成关卡或分享功能。
- 题目标题、答案和报告内容使用接口数据，因此随主题变化，不硬编码原型中的 RAG 示例。

## 验证证据

- `npm run typecheck` 通过。跳过第三方声明文件检查，应用源码继续执行类型检查。
- `npm test`：4 项通过，覆盖多选完全匹配、选择切换、重复/空提交、XP 与用时统计。
- `npm run build:weapp` 通过，生成 `frontend/dist`。
- H5 实际浏览器：375×812 下检查全部六状态；320×700 下检查长题干与长选项换行和滚动。
- 固定测试题库中完成单选、多选、判断等 5 题，答对 4 题，报告显示 80% 与 80 XP。
- 核对案例填充、取消生成后保留输入、讲解展开/折叠、提交按钮从不可用转为黄色。
- 实测题干字号 19px、可见顶部图标 30×30px、按钮颜色 rgb(255,216,77)，无横向溢出。
- 浏览器验收未记录运行错误；H5 构建仅有入口包大小提示。

浏览器验收使用 `frontend/tests/preview_server.py` 提供的固定响应，未调用 DeepSeek；真实小程序仍请求原有 localhost:8000 后端。本次没有修改后端业务，也没有重新验证模型出题质量。微信端已完成编译与生成样式检查，尚未在微信模拟器/真机里进行视觉验收。

## 查看更新

在 frontend 中停止旧 watcher，重新执行 `npm run dev:weapp`，然后在微信开发者工具重新编译 frontend 项目（该项目的 miniprogramRoot 指向 dist）。修改配置后必须重启 watcher。
