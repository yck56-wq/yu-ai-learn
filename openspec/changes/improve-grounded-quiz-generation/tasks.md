## 1. 检索边界与配置

- [x] 1.1 锁定 `langchain-tavily`、`langgraph` 与现有 LangChain 依赖的兼容版本，选定 Tavily 资料策略，记录 API 凭证环境变量、超时、最大结果数、最大工具调用次数和时效窗口；验证配置缺失时应用能启动但请求返回稳定的配置错误
- [x] 1.2 定义统一来源模型和 Tavily 适配器接口，提供 Search 轻量摘要、Search 深度原文和 Extract URL 三个工具实例；明确 `max_results`/`include_raw_content` 的实例化约束以及运行时 `search_depth`、`time_range`、日期、域名、`country` 和 `urls` 参数；验证工具可用模拟结果运行
- [x] 1.3 实现 URL 规范化、去重、主题相关性筛选、来源最低数量和摘要长度限制；验证重复 URL、无关结果和空结果测试通过

## 2. 题库领域模型与 API 契约

- [x] 2.1 扩展后端 Quiz/Question 模型，加入可选 `sources`、检索状态和题目级 `source_ids`，保持现有字段与题型兼容；验证现有题库 API 测试和 Pydantic 校验通过
- [x] 2.2 扩展题库持久化快照及历史读取，保存生成时来源元数据；验证服务重启或再次查询时来源不依赖实时检索
- [x] 2.3 定义检索失败、来源不足、主题歧义、证据冲突和模型质量失败的稳定日志/响应映射；验证检索异常或来源不足均可降级且日志与错误响应不包含密钥、完整模型输出或用户凭证

## 3. 有来源出题链路

- [x] 3.1 使用 LangChain `create_agent` 实现知识获取 Agent，将 Search/Extract 工具全部提供给 Agent，支持关键词搜索、深度搜索、URL 提取、最多两次工具调用和受限参数调整；验证 Agent 对主题、复杂主题、简单主题和 URL 输入选择预期工具
- [x] 3.2 在出题服务中加入主题时效性判断和 Agent 编排：可检索主题优先获取资料，Agent/搜索/提取异常时捕获、记录日志并走原 Prompt 无上下文降级；验证成功检索和异常降级分别进入预期分支
- [x] 3.3 将不超过 3000 字符的筛选来源摘要、来源 ID 注入版本化 ChatPromptTemplate，并保留用户输入作为不可信学习材料；验证提示词包含来源证据和忽略输入指令的约束
- [x] 3.4 增加题目级证据约束校验，检查 source ID 存在、关键断言有证据覆盖、冲突来源被限定或拒绝；验证偏题、无来源断言和冲突资料均不会返回成功题库
- [x] 3.5 保留并整合现有题量、选项、答案、知识点覆盖、重复题和有限重试逻辑；验证现有 `backend/tests/test_quiz_generation.py` 全部通过且新增质量测试覆盖 Agent/Tavily 重试、URL 提取和检索失败

## 4. 前端来源与失败交互

- [x] 4.1 更新前端题库类型和 API 解码，兼容可选来源字段；验证 `npm run typecheck` 与现有前端测试通过
- [x] 4.2 在生成结果或报告入口增加可折叠来源展示（标题、来源名、链接/可访问地址和时间）；验证旧题库无来源时页面仍可答题
- [x] 4.3 为模型质量失败增加明确中文提示与重试入口；检索超时或搜索异常降级时保持正常生成体验，保留用户输入；验证 H5 预览和微信构建均无新增编译错误

## 5. 联调与上线闸门

- [x] 5.1 增加 Tavily Search/Extract 适配器、Agent 工具选择、证据校验、降级日志和错误映射的单元/集成测试，使用脱敏固定样例覆盖 Harness Engineering 等新主题及搜索异常回归；验证后端 pytest 全部通过
- [x] 5.2 使用模拟检索与模型响应完成端到端生成、持久化、历史回看和旧客户端兼容验证；验证来源快照、题目结构和错误码符合规格
- [x] 5.3 在测试环境启用可配置开关并记录检索耗时、来源数量、拒绝原因等非敏感指标；验证异常时切回旧路径不会破坏现有题库生成


